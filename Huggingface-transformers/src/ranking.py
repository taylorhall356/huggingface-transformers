def compute_similarity(query, documents):
   
    similarities = [0.5, 0.3, 0.8]  # Placeholder values, 
    return similarities

# Incomplete part: the ranking function should sort documents by similarity, but leave it blank/incorrect
def rank_documents(query, documents):
    similarities = compute_similarity(query, documents)
    # TODO: Sort documents by similarities descending
    return documents  # Wrong: returns unsorted documents